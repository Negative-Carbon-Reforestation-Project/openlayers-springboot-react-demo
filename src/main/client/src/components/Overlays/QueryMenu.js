import usePopup from "./usePopup";

/**
 * Container for the Query Menu
 * @returns {JSX.Element}
 */
const QueryMenu = () => {
    // usePopup contains the content of the popup
    const { popupRef, popupCloseButtonRef, popupContent } = usePopup();

    return (
        <section>

        </section>
    )
}